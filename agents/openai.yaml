interface:
  display_name: "Claude Review"
  short_description: "Run Claude Code for review-only or iterate-fix-review loops"
  default_prompt: "Use /claude review plan <path>, /claude review plan, or /claude review code for report-only review. Use /claude review iterate for a bounded fix-and-rereview loop."

policy:
  allow_implicit_invocation: true
